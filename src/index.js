import ReactDOM from 'react-dom'
import App from './App'
import './index.css'

// to display React elements of components on the web page, then you can use ReactDOM.render () method.
ReactDOM.render(
<App/>,document.querySelector("#root")
// it means components of App is desplayed in DOM where id=root
)